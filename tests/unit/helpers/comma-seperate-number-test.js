import { commaSeperateNumber } from 'merlin/helpers/comma-seperate-number';
import { module, test } from 'qunit';

module('Unit | Helper | comma seperate number');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = commaSeperateNumber([42]);
  assert.ok(result);
});
