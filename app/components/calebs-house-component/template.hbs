{{lemonade-chart type='line' data=this.graphs.testGraph.data options=this.graphs.testGraph.options legend=true}}

{{!-- <div class='timeline-track-container'>
  {{timeline-track
    timelineGridObjects=this.timelineGridObjects
    timelineObjects=this.timelineObjects
    onInteractionEnd=(action 'onInteractionEnd')
  }}
  {{timeline-track
    timelineGridObjects=this.timelineGridObjects
    timelineObjects=this.timelineObjects
    onInteractionEnd=(action 'onInteractionEnd')
  }}
  {{timeline-track
    timelineGridObjects=this.timelineGridObjects
    timelineObjects=this.timelineObjects
    onInteractionEnd=(action 'onInteractionEnd')
  }}
  {{timeline-track
    timelineGridObjects=this.timelineGridObjects
    timelineObjects=this.timelineObjects
    onInteractionEnd=(action 'onInteractionEnd')
  }}
</div>

{{timeline-timescale
  timelineGridObjects=this.timelineGridObjects
  timespan=this.timespan
}} --}}
